HARD RULES:
1. when you are told to reference to any files in `ref/*` 
    - you can:
        - take reference to the logic
        - copy the code and reuse it somewhere
    - you cannot:
        - import any function/variables etc from any files in `ref/*`
